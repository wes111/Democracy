//
//  Rule+Preview.swift
//  Democracy
//
//  Created by Wesley Luntsford on 6/22/23.
//

import Foundation

extension Rule {
    static let previewArray: [Rule] = [
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        ),
        Rule(
            id: UUID().uuidString,
            title: "Do not block Moderators or Admins.",
            description: """
                         You're not being clever by blocking mods or admins, we can still find
                         you through the look up member function, or ask another mod to block
                         you for us. Blocking modmins results in an immediate ban.
                         """
        )
    ]
}
